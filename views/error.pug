extends layout.pug

block content
  section.error
    h1= `I am terrebly sorry, but the error had occured! ${error.message} ${error.status}`
    .error-stack
      pre.stack-trace= error.stack
    br
    a.button(href='/') Go Back To Menu
